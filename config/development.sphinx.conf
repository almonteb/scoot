indexer
{
}

searchd
{
  address = 127.0.0.1
  port = 3312
  log = /Users/radar/Sites/gitorious/log/searchd.log
  query_log = /Users/radar/Sites/gitorious/log/searchd.query.log
  pid_file = /Users/radar/Sites/gitorious/log/searchd.development.pid
}

source comment_core_0
{
  type = mysql
  sql_host = localhost
  sql_user = root
  sql_pass = 
  sql_db = gitorious
  sql_query_pre = SET NAMES utf8
  sql_query = SELECT `comments`.`id` * 4 + 0 AS `id` , CAST(`comments`.`body` AS CHAR) AS `body`, CAST(`users`.`login` AS CHAR) AS `user`, `comments`.`id` AS `sphinx_internal_id`, 1539927024 AS `class_crc`, '1539927024' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM comments    LEFT OUTER JOIN `users` ON `users`.id = `comments`.user_id  WHERE `comments`.`id` >= $start   AND `comments`.`id` <= $end    GROUP BY `comments`.`id`  ORDER BY NULL
  sql_query_range = SELECT IFNULL(MIN(`id`), 1), IFNULL(MAX(`id`), 1) FROM `comments` 
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = class_crc
  sql_attr_uint = sphinx_deleted
  sql_attr_multi = uint subclass_crcs from field
  sql_query_info = SELECT * FROM `comments` WHERE  `id` = (($id - 0) / 4)
}

index comment_core
{
  source = comment_core_0
  path = /Users/radar/Sites/gitorious/db/sphinx/development/comment_core
  morphology = stem_en
  charset_type = utf-8
}

index comment
{
  type = distributed
  local = comment_core
}

source merge_request_core_0
{
  type = mysql
  sql_host = localhost
  sql_user = root
  sql_pass = 
  sql_db = gitorious
  sql_query_pre = SET NAMES utf8
  sql_query = SELECT `merge_requests`.`id` * 4 + 1 AS `id` , CAST(`merge_requests`.`proposal` AS CHAR) AS `proposal`, CAST(`users`.`login` AS CHAR) AS `user`, `merge_requests`.`id` AS `sphinx_internal_id`, 3479778401 AS `class_crc`, '3479778401' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM merge_requests    LEFT OUTER JOIN `users` ON `users`.id = `merge_requests`.user_id  WHERE `merge_requests`.`id` >= $start   AND `merge_requests`.`id` <= $end    GROUP BY `merge_requests`.`id`  ORDER BY NULL
  sql_query_range = SELECT IFNULL(MIN(`id`), 1), IFNULL(MAX(`id`), 1) FROM `merge_requests` 
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = class_crc
  sql_attr_uint = sphinx_deleted
  sql_attr_multi = uint subclass_crcs from field
  sql_query_info = SELECT * FROM `merge_requests` WHERE  `id` = (($id - 1) / 4)
}

index merge_request_core
{
  source = merge_request_core_0
  path = /Users/radar/Sites/gitorious/db/sphinx/development/merge_request_core
  morphology = stem_en
  charset_type = utf-8
}

index merge_request
{
  type = distributed
  local = merge_request_core
}

source project_core_0
{
  type = mysql
  sql_host = localhost
  sql_user = root
  sql_pass = 
  sql_db = gitorious
  sql_query_pre = SET NAMES utf8
  sql_query = SELECT `projects`.`id` * 4 + 2 AS `id` , CAST(`projects`.`title` AS CHAR) AS `title`, CAST(`projects`.`description` AS CHAR) AS `description`, CAST(`projects`.`slug` AS CHAR) AS `slug`, CAST(`users`.`login` AS CHAR) AS `user`, `projects`.`id` AS `sphinx_internal_id`, 3759073650 AS `class_crc`, '3759073650' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM projects    LEFT OUTER JOIN `users` ON `users`.id = `projects`.user_id  WHERE `projects`.`id` >= $start   AND `projects`.`id` <= $end    GROUP BY `projects`.`id`  ORDER BY NULL
  sql_query_range = SELECT IFNULL(MIN(`id`), 1), IFNULL(MAX(`id`), 1) FROM `projects` 
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = class_crc
  sql_attr_uint = sphinx_deleted
  sql_attr_multi = uint subclass_crcs from field
  sql_query_info = SELECT * FROM `projects` WHERE  `id` = (($id - 2) / 4)
}

index project_core
{
  source = project_core_0
  path = /Users/radar/Sites/gitorious/db/sphinx/development/project_core
  morphology = stem_en
  charset_type = utf-8
}

index project
{
  type = distributed
  local = project_core
}

source user_core_0
{
  type = mysql
  sql_host = localhost
  sql_user = root
  sql_pass = 
  sql_db = gitorious
  sql_query_pre = SET NAMES utf8
  sql_query = SELECT `users`.`id` * 4 + 3 AS `id` , CAST(`users`.`login` AS CHAR) AS `login`, `users`.`id` AS `sphinx_internal_id`, 765557111 AS `class_crc`, '765557111' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM users    WHERE `users`.`id` >= $start   AND `users`.`id` <= $end    GROUP BY `users`.`id`  ORDER BY NULL
  sql_query_range = SELECT IFNULL(MIN(`id`), 1), IFNULL(MAX(`id`), 1) FROM `users` 
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = class_crc
  sql_attr_uint = sphinx_deleted
  sql_attr_multi = uint subclass_crcs from field
  sql_query_info = SELECT * FROM `users` WHERE  `id` = (($id - 3) / 4)
}

index user_core
{
  source = user_core_0
  path = /Users/radar/Sites/gitorious/db/sphinx/development/user_core
  morphology = stem_en
  charset_type = utf-8
}

index user
{
  type = distributed
  local = user_core
}
